# Lombok examples
In this package you will find annotation usage examples of Lombok.

##Setup
The examples are split into different packages one good use of annotations, one for bad usage and one without the use of lombok annotations. Foreach example tests are added to give proof of work. 
